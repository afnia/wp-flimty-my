- hosts: webserver
  become: true
  become_user: root
  tasks:
    - name: Pull docker
      command: docker-compose pull
      args:
        chdir: /home/wordpress/caradietmudah.com

    - name: Run docker-compose
      command: docker-compose up --build -d production
      args:
        chdir: /home/wordpress/caradietmudah.com
